-- WARNING: This schema is for context only and is not meant to be run.
-- Table order and constraints may not be valid for execution.

CREATE TABLE public.active_sessions (
  id integer NOT NULL DEFAULT nextval('active_sessions_id_seq'::regclass),
  counsellor_id character varying NOT NULL,
  token text NOT NULL,
  device_info character varying,
  ip_address character varying,
  last_activity timestamp with time zone,
  created_at timestamp with time zone,
  CONSTRAINT active_sessions_pkey PRIMARY KEY (id)
);
CREATE TABLE public.admission_assignment_rulesets (
  admission_assignment_rulesets_id integer NOT NULL DEFAULT nextval('admission_assignment_rulesets_admission_assignment_rulesets_seq'::regclass),
  name character varying,
  college character varying DEFAULT ''::character varying,
  university_name ARRAY DEFAULT ARRAY[]::text[],
  course_conditions jsonb DEFAULT '{}'::jsonb,
  source ARRAY DEFAULT ARRAY[]::text[],
  assigned_counsellor_ids ARRAY DEFAULT ARRAY[]::text[],
  is_active boolean DEFAULT true,
  priority integer DEFAULT 0,
  round_robin_index integer DEFAULT 0,
  created_at timestamp with time zone,
  updated_at timestamp with time zone,
  custom_rule_name character varying DEFAULT ''::character varying,
  CONSTRAINT admission_assignment_rulesets_pkey PRIMARY KEY (admission_assignment_rulesets_id)
);
CREATE TABLE public.ai_query_histories (
  query_id integer NOT NULL DEFAULT nextval('ai_query_histories_query_id_seq'::regclass),
  user_id character varying,
  user_role character varying,
  natural_language_prompt text NOT NULL,
  generated_sql text NOT NULL,
  execution_status character varying DEFAULT 'pending'::character varying,
  result_count integer,
  execution_time_ms integer,
  feedback_status character varying DEFAULT 'pending'::character varying,
  user_feedback text,
  corrected_sql text,
  similarity_score double precision,
  created_at timestamp with time zone,
  updated_at timestamp with time zone,
  CONSTRAINT ai_query_histories_pkey PRIMARY KEY (query_id)
);
CREATE TABLE public.amity_request_and_response (
  id integer NOT NULL DEFAULT nextval('amity_request_and_response_id_seq'::regclass),
  student_name character varying NOT NULL,
  student_email character varying NOT NULL,
  student_phone character varying NOT NULL,
  source character varying DEFAULT 'nuvora'::character varying,
  source_medium character varying NOT NULL,
  course character varying NOT NULL,
  source_campaign character varying NOT NULL,
  state character varying NOT NULL,
  city character varying NOT NULL,
  campus character varying NOT NULL,
  response_message_id character varying,
  response_message_related_id character varying,
  response_message_is_created boolean,
  response_status character varying,
  lead_generated_by character varying NOT NULL,
  created_at timestamp with time zone,
  CONSTRAINT amity_request_and_response_pkey PRIMARY KEY (id)
);
CREATE TABLE public.analyser_users (
  analyser_id character varying NOT NULL,
  analyser_name character varying NOT NULL,
  analyser_email character varying NOT NULL UNIQUE,
  analyser_password character varying NOT NULL,
  analyser_last_login timestamp with time zone,
  created_at timestamp with time zone,
  updated_at timestamp with time zone,
  CONSTRAINT analyser_users_pkey PRIMARY KEY (analyser_id)
);
CREATE TABLE public.analysers (
  id character varying NOT NULL,
  name character varying NOT NULL,
  email character varying NOT NULL UNIQUE,
  password character varying NOT NULL,
  sources json DEFAULT '[]'::json,
  source_urls json DEFAULT '[]'::json,
  campaigns json DEFAULT '[]'::json,
  last_login timestamp with time zone,
  created_at timestamp with time zone NOT NULL,
  updated_at timestamp with time zone NOT NULL,
  student_creation_date text,
  role text,
  CONSTRAINT analysers_pkey PRIMARY KEY (id)
);
CREATE TABLE public.campaign_id_mapping (
  id integer NOT NULL DEFAULT nextval('campaign_id_mapping_id_seq'::regclass),
  campign_id character varying NOT NULL UNIQUE,
  campign_name character varying,
  state character varying NOT NULL,
  stream character varying NOT NULL,
  degree character varying NOT NULL,
  mode USER-DEFINED NOT NULL,
  created_at timestamp with time zone NOT NULL,
  updated_at timestamp with time zone NOT NULL,
  CONSTRAINT campaign_id_mapping_pkey PRIMARY KEY (id)
);
CREATE TABLE public.chats (
  chat_id character varying NOT NULL,
  participants ARRAY NOT NULL,
  initiated_by character varying,
  is_locked boolean DEFAULT true,
  unread_count jsonb DEFAULT '{}'::jsonb,
  last_message_time timestamp with time zone,
  created_at timestamp with time zone,
  CONSTRAINT chats_pkey PRIMARY KEY (chat_id)
);
CREATE TABLE public.counsellor_break_logs (
  id integer NOT NULL DEFAULT nextval('counsellor_break_logs_id_seq'::regclass),
  counsellor_id character varying NOT NULL,
  break_start timestamp with time zone NOT NULL,
  break_end timestamp with time zone,
  duration integer,
  duration_seconds integer,
  duration_formatted character varying,
  created_at timestamp with time zone NOT NULL,
  updated_at timestamp with time zone NOT NULL,
  break_type character varying DEFAULT ''::character varying,
  notes character varying DEFAULT ''::character varying,
  CONSTRAINT counsellor_break_logs_pkey PRIMARY KEY (id)
);
CREATE TABLE public.counsellor_sessions (
  session_id character varying NOT NULL,
  counsellor_id character varying NOT NULL,
  session_token character varying NOT NULL UNIQUE,
  ip_address character varying,
  user_agent text,
  device_type character varying,
  browser_name character varying,
  browser_version character varying,
  os_name character varying,
  os_version character varying,
  location character varying,
  is_active boolean DEFAULT true,
  login_time timestamp with time zone,
  logout_time timestamp with time zone,
  last_activity timestamp with time zone,
  created_at timestamp with time zone,
  updated_at timestamp with time zone,
  CONSTRAINT counsellor_sessions_pkey PRIMARY KEY (session_id),
  CONSTRAINT counsellor_sessions_counsellor_id_fkey FOREIGN KEY (counsellor_id) REFERENCES public.counsellors(counsellor_id)
);
CREATE TABLE public.counsellors (
  counsellor_id character varying NOT NULL,
  counsellor_name character varying,
  counsellor_email character varying UNIQUE,
  counsellor_password character varying,
  status character varying DEFAULT 'active'::character varying,
  role character varying,
  counsellor_preferred_mode character varying DEFAULT 'Regular'::character varying,
  total_leads integer DEFAULT 0,
  current_lead_capacity integer DEFAULT 0,
  counsellor_last_login timestamp with time zone,
  is_logout boolean DEFAULT false,
  created_at timestamp with time zone,
  updated_at timestamp with time zone,
  assigned_to character varying DEFAULT NULL::character varying,
  CONSTRAINT counsellors_pkey PRIMARY KEY (counsellor_id)
);
CREATE TABLE public.course_status_journeys (
  status_history_id integer NOT NULL DEFAULT nextval('course_status_journeys_status_history_id_seq'::regclass),
  student_id character varying,
  course_id character varying,
  counsellor_id character varying,
  course_status character varying,
  deposit_amount numeric DEFAULT 0,
  currency character varying DEFAULT 'INR'::character varying,
  exam_interview_date timestamp with time zone,
  last_admission_date timestamp with time zone,
  notes text,
  created_at timestamp with time zone,
  fee_type character varying,
  CONSTRAINT course_status_journeys_pkey PRIMARY KEY (status_history_id),
  CONSTRAINT course_status_journeys_course_id_fkey FOREIGN KEY (course_id) REFERENCES public.university_courses(course_id),
  CONSTRAINT course_status_journeys_student_id_fkey FOREIGN KEY (student_id) REFERENCES public.students(student_id)
);
CREATE TABLE public.cu_request_and_response (
  id integer NOT NULL DEFAULT nextval('cu_request_and_response_id_seq'::regclass),
  student_name character varying NOT NULL,
  student_email character varying NOT NULL UNIQUE,
  student_phone character varying NOT NULL,
  source character varying DEFAULT 'Nuvora'::character varying,
  mx_program_code_new character varying NOT NULL,
  mx_program_new character varying NOT NULL,
  mx_discipline_new character varying NOT NULL,
  state character varying NOT NULL,
  city character varying NOT NULL,
  date_of_birth character varying NOT NULL,
  campus character varying NOT NULL,
  response_message_id character varying,
  response_message_related_id character varying,
  response_message_is_created boolean,
  response_status character varying,
  lead_generated_by character varying NOT NULL,
  created_at timestamp with time zone,
  CONSTRAINT cu_request_and_response_pkey PRIMARY KEY (id)
);
CREATE TABLE public.filter_options (
  id integer NOT NULL DEFAULT nextval('filter_options_id_seq'::regclass),
  mode ARRAY DEFAULT ARRAY[]::text[],
  source ARRAY DEFAULT ARRAY[]::text[],
  first_source_url ARRAY DEFAULT ARRAY[]::text[],
  lead_status ARRAY DEFAULT ARRAY[]::text[],
  sub_lead_status ARRAY DEFAULT ARRAY[]::text[],
  calling_status ARRAY DEFAULT ARRAY[]::text[],
  calling_sub_status ARRAY DEFAULT ARRAY[]::text[],
  campaign_name ARRAY DEFAULT ARRAY[]::text[],
  created_at timestamp with time zone,
  updated_at timestamp with time zone,
  CONSTRAINT filter_options_pkey PRIMARY KEY (id)
);
CREATE TABLE public.follow_up_status (
  id integer NOT NULL DEFAULT nextval('follow_up_status_id_seq'::regclass),
  student_id character varying,
  google_calendar_event_id character varying,
  created_by character varying,
  status character varying,
  gmail_status character varying,
  sms_status character varying,
  meet_time character varying,
  meet_date character varying,
  google_meet_url character varying,
  created_at timestamp with time zone,
  updated_at timestamp with time zone,
  CONSTRAINT follow_up_status_pkey PRIMARY KEY (id)
);
CREATE TABLE public.google_ads_leads (
  id integer NOT NULL DEFAULT nextval('google_ads_leads_id_seq'::regclass),
  created_time timestamp with time zone NOT NULL,
  full_name character varying DEFAULT NULL::character varying,
  email character varying DEFAULT NULL::character varying,
  phone_number character varying DEFAULT NULL::character varying,
  city character varying DEFAULT NULL::character varying,
  form_id character varying NOT NULL,
  additional_fields jsonb,
  campaign_id character varying DEFAULT ''::character varying,
  created_at timestamp with time zone,
  CONSTRAINT google_ads_leads_pkey PRIMARY KEY (id)
);
CREATE TABLE public.ivr_calls (
  id integer NOT NULL DEFAULT nextval('ivr_calls_id_seq'::regclass),
  name character varying DEFAULT 'NA'::character varying,
  email character varying,
  phone_number character varying,
  source character varying DEFAULT 'IVR'::character varying,
  first_source_url character varying,
  destination_number character varying,
  dial_whom_number character varying,
  call_duration integer,
  status character varying,
  start_time timestamp with time zone,
  end_time timestamp with time zone,
  call_sid character varying,
  call_recording_url text,
  talk_duration integer,
  created_at timestamp with time zone NOT NULL,
  updated_at timestamp with time zone NOT NULL,
  CONSTRAINT ivr_calls_pkey PRIMARY KEY (id)
);
CREATE TABLE public.l2_assignment_rulesets (
  lead_assignment_rule_l2_id integer NOT NULL DEFAULT nextval('l2_assignment_rulesets_lead_assignment_rule_l2_id_seq'::regclass),
  name character varying NOT NULL UNIQUE,
  conditions jsonb DEFAULT '{}'::jsonb,
  assigned_counsellor_ids ARRAY DEFAULT ARRAY[]::text[],
  is_active boolean DEFAULT true,
  priority integer DEFAULT 0,
  round_robin_index integer DEFAULT 0,
  created_at timestamp with time zone,
  updated_at timestamp with time zone,
  custom_rule_name character varying DEFAULT ''::character varying,
  description text DEFAULT ''::text,
  total_matched_leads integer DEFAULT 0,
  last_matched_at timestamp without time zone,
  CONSTRAINT l2_assignment_rulesets_pkey PRIMARY KEY (lead_assignment_rule_l2_id)
);
CREATE TABLE public.l3_assignment_rulesets (
  l3_assignment_rulesets_id integer NOT NULL DEFAULT nextval('l3_assignment_rulesets_l3_assignment_rulesets_id_seq'::regclass),
  name character varying,
  college character varying DEFAULT ''::character varying,
  university_name ARRAY DEFAULT ARRAY[]::text[],
  course_conditions jsonb DEFAULT '{}'::jsonb,
  source ARRAY DEFAULT ARRAY[]::text[],
  assigned_counsellor_ids ARRAY DEFAULT ARRAY[]::text[],
  is_active boolean DEFAULT true,
  priority integer DEFAULT 0,
  round_robin_index integer DEFAULT 0,
  created_at timestamp with time zone,
  updated_at timestamp with time zone,
  custom_rule_name character varying DEFAULT ''::character varying,
  CONSTRAINT l3_assignment_rulesets_pkey PRIMARY KEY (l3_assignment_rulesets_id)
);
CREATE TABLE public.last_assign_online (
  id integer NOT NULL DEFAULT nextval('last_assign_online_id_seq'::regclass),
  counsellor_id character varying NOT NULL,
  created_at timestamp with time zone,
  updated_at timestamp with time zone,
  CONSTRAINT last_assign_online_pkey PRIMARY KEY (id)
);
CREATE TABLE public.last_assign_regular (
  id integer NOT NULL DEFAULT nextval('last_assign_regular_id_seq'::regclass),
  counsellor_id character varying NOT NULL,
  created_at timestamp with time zone,
  updated_at timestamp with time zone,
  CONSTRAINT last_assign_regular_pkey PRIMARY KEY (id)
);
CREATE TABLE public.latest_course_statuses (
  id integer NOT NULL DEFAULT nextval('latest_course_statuses_id_seq'::regclass),
  course_id character varying,
  student_id character varying,
  created_by character varying,
  latest_course_status character varying DEFAULT 'Shortlisted'::character varying,
  is_shortlisted boolean DEFAULT false,
  created_at timestamp with time zone,
  college_api_sent_status character varying DEFAULT ''::character varying,
  updated_at timestamp with time zone,
  CONSTRAINT latest_course_statuses_pkey PRIMARY KEY (id),
  CONSTRAINT latest_course_statuses_course_id_fkey FOREIGN KEY (course_id) REFERENCES public.university_courses(course_id),
  CONSTRAINT latest_course_statuses_student_id_fkey FOREIGN KEY (student_id) REFERENCES public.students(student_id)
);
CREATE TABLE public.lead_assignment_log (
  id integer NOT NULL DEFAULT nextval('lead_assignment_log_id_seq'::regclass),
  student_id character varying NOT NULL,
  assigned_counsellor_id character varying NOT NULL,
  assigned_by character varying DEFAULT NULL::character varying,
  reference_from character varying DEFAULT NULL::character varying,
  created_at timestamp with time zone DEFAULT '2025-08-05 15:06:59.174+00'::timestamp with time zone,
  CONSTRAINT lead_assignment_log_pkey PRIMARY KEY (id),
  CONSTRAINT lead_assignment_log_student_id_fkey FOREIGN KEY (student_id) REFERENCES public.students(student_id)
);
CREATE TABLE public.login_attempts (
  id bigint NOT NULL DEFAULT nextval('login_attempts_id_seq'::regclass),
  user_type character varying NOT NULL,
  user_id character varying,
  user_name character varying,
  success boolean DEFAULT false,
  ip_address character varying,
  created_at timestamp with time zone NOT NULL,
  meta json,
  CONSTRAINT login_attempts_pkey PRIMARY KEY (id)
);
CREATE TABLE public.lpu_request_and_response (
  id integer NOT NULL DEFAULT nextval('lpu_request_and_response_id_seq'::regclass),
  student_name character varying NOT NULL,
  student_email character varying NOT NULL UNIQUE,
  student_phone character varying NOT NULL UNIQUE,
  college_id character varying NOT NULL,
  college_name character varying NOT NULL,
  lead_generated_by character varying NOT NULL,
  field_program character varying NOT NULL,
  field_session character varying NOT NULL,
  state character varying NOT NULL,
  response_message character varying NOT NULL,
  response_status character varying NOT NULL,
  created_at timestamp with time zone,
  CONSTRAINT lpu_request_and_response_pkey PRIMARY KEY (id)
);
CREATE TABLE public.messages (
  chat_id character varying NOT NULL,
  message_id character varying NOT NULL,
  message text NOT NULL,
  message_type USER-DEFINED DEFAULT 'text'::enum_messages_message_type,
  sender character varying NOT NULL,
  receiver character varying NOT NULL,
  direction USER-DEFINED NOT NULL,
  is_read boolean DEFAULT false,
  read_at timestamp with time zone,
  timestamp timestamp with time zone,
  CONSTRAINT messages_pkey PRIMARY KEY (message_id),
  CONSTRAINT messages_chat_id_fkey FOREIGN KEY (chat_id) REFERENCES public.chats(chat_id)
);
CREATE TABLE public.meta_ads_leads (
  id integer NOT NULL DEFAULT nextval('meta_ads_leads_id_seq'::regclass),
  created_time timestamp with time zone NOT NULL,
  full_name character varying DEFAULT NULL::character varying,
  email character varying DEFAULT NULL::character varying,
  phone_number character varying DEFAULT NULL::character varying,
  city character varying DEFAULT NULL::character varying,
  form_id character varying NOT NULL,
  additional_fields jsonb,
  campaign_name character varying DEFAULT ''::character varying,
  source_url character varying DEFAULT ''::character varying,
  created_at timestamp with time zone,
  CONSTRAINT meta_ads_leads_pkey PRIMARY KEY (id)
);
CREATE TABLE public.meta_ads_tokens (
  id integer NOT NULL DEFAULT nextval('meta_ads_tokens_id_seq'::regclass),
  page_id character varying,
  page_access_token text,
  long_lived_user_token text,
  created_at timestamp with time zone NOT NULL,
  updated_at timestamp with time zone NOT NULL,
  CONSTRAINT meta_ads_tokens_pkey PRIMARY KEY (id)
);
CREATE TABLE public.meta_audiences (
  id integer NOT NULL DEFAULT nextval('meta_audiences_id_seq'::regclass),
  group_name character varying NOT NULL UNIQUE,
  meta_audience_id character varying NOT NULL UNIQUE,
  ad_account_id character varying NOT NULL,
  audience_type USER-DEFINED DEFAULT 'SYSTEM'::enum_meta_audiences_audience_type,
  status USER-DEFINED DEFAULT 'ACTIVE'::enum_meta_audiences_status,
  lead_count integer NOT NULL DEFAULT 0,
  created_at timestamp with time zone NOT NULL,
  updated_at timestamp with time zone NOT NULL,
  CONSTRAINT meta_audiences_pkey PRIMARY KEY (id)
);
CREATE TABLE public.meta_event_logs (
  id bigint NOT NULL DEFAULT nextval('meta_event_logs_id_seq'::regclass),
  email character varying NOT NULL,
  phone character varying NOT NULL,
  event_name character varying NOT NULL,
  event_id character varying NOT NULL,
  fired boolean DEFAULT true,
  created_at timestamp with time zone NOT NULL,
  updated_at timestamp with time zone NOT NULL,
  source character varying DEFAULT 'Facebook'::character varying,
  CONSTRAINT meta_event_logs_pkey PRIMARY KEY (id)
);
CREATE TABLE public.recon_assignment_rulesets (
  lead_assignment_rule_recon_id integer NOT NULL DEFAULT nextval('recon_assignment_rulesets_lead_assignment_rule_recon_id_seq'::regclass),
  name character varying NOT NULL UNIQUE,
  conditions jsonb DEFAULT '{}'::jsonb,
  assigned_university_names ARRAY DEFAULT ARRAY[]::text[],
  is_active boolean DEFAULT true,
  priority integer DEFAULT 0,
  round_robin_index integer DEFAULT 0,
  total_matched_leads integer DEFAULT 0,
  last_matched_at timestamp with time zone,
  created_at timestamp with time zone,
  updated_at timestamp with time zone,
  custom_rule_name character varying DEFAULT ''::character varying,
  CONSTRAINT recon_assignment_rulesets_pkey PRIMARY KEY (lead_assignment_rule_recon_id)
);
CREATE TABLE public.regular_db_ruleset (
  id integer NOT NULL DEFAULT nextval('regular_db_ruleset_id_seq'::regclass),
  campaign_id ARRAY NOT NULL,
  created_by character varying,
  history json DEFAULT '[]'::json,
  created_at timestamp with time zone NOT NULL,
  updated_at timestamp with time zone NOT NULL,
  CONSTRAINT regular_db_ruleset_pkey PRIMARY KEY (id)
);
CREATE TABLE public.secondary_student_infos (
  id integer NOT NULL DEFAULT nextval('secondary_student_infos_id_seq'::regclass),
  student_id character varying NOT NULL,
  student_info json,
  created_at timestamp with time zone NOT NULL,
  updated_at timestamp with time zone NOT NULL,
  CONSTRAINT secondary_student_infos_pkey PRIMARY KEY (id)
);
CREATE TABLE public.student_college_api_sent_status (
  id integer NOT NULL DEFAULT nextval('student_college_api_sent_status_id_seq'::regclass),
  student_id character varying,
  college_name character varying,
  api_sent_status character varying,
  request_to_api jsonb,
  response_from_api jsonb,
  request_header_to_api jsonb,
  response_header_from_api jsonb,
  sent_type character varying DEFAULT 'manual'::character varying,
  created_at timestamp with time zone NOT NULL,
  updated_at timestamp with time zone NOT NULL,
  student_email character varying,
  student_phone character varying,
  is_primary boolean DEFAULT true,
  CONSTRAINT student_college_api_sent_status_pkey PRIMARY KEY (id),
  CONSTRAINT student_college_api_sent_status_student_id_fkey FOREIGN KEY (student_id) REFERENCES public.students(student_id)
);
CREATE TABLE public.student_college_credentials (
  id integer NOT NULL DEFAULT nextval('student_college_credentials_id_seq'::regclass),
  form_id character varying,
  coupon_code character varying,
  user_name character varying,
  password character varying,
  student_id character varying,
  counsellor_id character varying,
  course_id character varying,
  created_at timestamp with time zone NOT NULL,
  updated_at timestamp with time zone NOT NULL,
  supervisor_id character varying,
  CONSTRAINT student_college_credentials_pkey PRIMARY KEY (id),
  CONSTRAINT student_college_credentials_counsellor_id_fkey FOREIGN KEY (counsellor_id) REFERENCES public.counsellors(counsellor_id),
  CONSTRAINT student_college_credentials_course_id_fkey FOREIGN KEY (course_id) REFERENCES public.university_courses(course_id),
  CONSTRAINT student_college_credentials_student_id_fkey FOREIGN KEY (student_id) REFERENCES public.students(student_id)
);
CREATE TABLE public.student_college_sent_status (
  id integer NOT NULL DEFAULT nextval('student_college_sent_status_id_seq'::regclass),
  student_id character varying NOT NULL,
  status character varying,
  college_name character varying,
  CONSTRAINT student_college_sent_status_pkey PRIMARY KEY (id),
  CONSTRAINT student_college_sent_status_student_id_fkey FOREIGN KEY (student_id) REFERENCES public.students(student_id)
);
CREATE TABLE public.student_lead_activities (
  id integer NOT NULL DEFAULT nextval('student_lead_activities_id_seq'::regclass),
  student_id character varying,
  student_name character varying DEFAULT ''::character varying,
  student_email character varying DEFAULT ''::character varying,
  student_phone character varying DEFAULT ''::character varying,
  parents_number character varying DEFAULT ''::character varying,
  whatsapp character varying DEFAULT ''::character varying,
  cta_name character varying DEFAULT ''::character varying,
  form_name character varying DEFAULT ''::character varying,
  source character varying DEFAULT ''::character varying,
  source_url text DEFAULT ''::text,
  utm_source character varying DEFAULT ''::character varying,
  utm_medium character varying DEFAULT ''::character varying,
  utm_keyword character varying DEFAULT ''::character varying,
  utm_campaign character varying DEFAULT ''::character varying,
  utm_campaign_id character varying DEFAULT ''::character varying,
  utm_adgroup_id character varying DEFAULT ''::character varying,
  utm_creative_id character varying DEFAULT ''::character varying,
  ip_city character varying DEFAULT ''::character varying,
  browser character varying DEFAULT ''::character varying,
  device character varying DEFAULT ''::character varying,
  student_comment jsonb DEFAULT '[]'::jsonb,
  highest_qualification character varying DEFAULT ''::character varying,
  working_professional boolean DEFAULT false,
  student_status character varying DEFAULT 'new'::character varying,
  destination_number character varying DEFAULT ''::character varying,
  dial_whom_number character varying DEFAULT ''::character varying,
  call_duration character varying DEFAULT ''::character varying,
  ivr_status character varying DEFAULT ''::character varying,
  start_time character varying DEFAULT ''::character varying,
  end_time character varying DEFAULT ''::character varying,
  call_sid character varying DEFAULT ''::character varying,
  call_recording_url character varying DEFAULT ''::character varying,
  talk_duration character varying DEFAULT ''::character varying,
  created_at timestamp with time zone,
  updated_at timestamp with time zone,
  is_transfered boolean DEFAULT false,
  is_transfer boolean DEFAULT false,
  CONSTRAINT student_lead_activities_pkey PRIMARY KEY (id),
  CONSTRAINT student_lead_activities_student_id_fkey FOREIGN KEY (student_id) REFERENCES public.students(student_id)
);
CREATE TABLE public.student_post_admission_remarks (
  remark_id integer NOT NULL DEFAULT nextval('student_post_admission_remarks_remark_id_seq'::regclass),
  feesAmount integer DEFAULT 0,
  student_id character varying,
  counsellor_id character varying,
  supervisor_id character varying,
  isdisabled boolean DEFAULT false,
  lead_status character varying,
  lead_sub_status character varying,
  calling_status character varying,
  sub_calling_status character varying,
  remarks text,
  callback_date date,
  callback_time character varying,
  created_at timestamp with time zone DEFAULT CURRENT_TIMESTAMP,
  updated_at timestamp with time zone DEFAULT CURRENT_TIMESTAMP,
  CONSTRAINT student_post_admission_remarks_pkey PRIMARY KEY (remark_id)
);
CREATE TABLE public.student_reassignment_logic (
  id integer NOT NULL DEFAULT nextval('student_reassignment_logic_id_seq'::regclass),
  assignment_logic json NOT NULL,
  created_by character varying NOT NULL,
  created_at timestamp with time zone NOT NULL,
  updated_at timestamp with time zone NOT NULL,
  status character varying,
  student_created_to character varying,
  student_created_from character varying,
  last_assigned_counsellor_index integer DEFAULT 0,
  activity_logs json,
  CONSTRAINT student_reassignment_logic_pkey PRIMARY KEY (id)
);
CREATE TABLE public.student_remarks (
  remark_id integer NOT NULL DEFAULT nextval('student_remarks_remark_id_seq'::regclass),
  student_id character varying,
  counsellor_id character varying,
  lead_status character varying,
  lead_sub_status character varying,
  calling_status character varying,
  sub_calling_status character varying,
  remarks text,
  callback_date date,
  callback_time character varying,
  updated_at timestamp with time zone,
  created_at timestamp with time zone,
  supervisor_id character varying,
  isdisabled boolean DEFAULT false,
  feesamount integer DEFAULT 0,
  feesAmount integer DEFAULT 0
);
CREATE TABLE public.student_remarks_dump (
  remark_id integer NOT NULL DEFAULT nextval('student_remarks_dump_remark_id_seq'::regclass),
  student_id character varying,
  counsellor_id character varying,
  lead_status character varying,
  lead_sub_status character varying,
  calling_status character varying,
  sub_calling_status character varying,
  remarks text,
  callback_date date,
  callback_time character varying,
  updated_at timestamp without time zone DEFAULT CURRENT_TIMESTAMP,
  created_at timestamp without time zone DEFAULT CURRENT_TIMESTAMP,
  CONSTRAINT student_remarks_dump_pkey PRIMARY KEY (remark_id)
);
CREATE TABLE public.student_whishlist (
  id integer NOT NULL DEFAULT nextval('student_whishlist_id_seq'::regclass),
  student_id character varying NOT NULL,
  counsellor_id character varying NOT NULL,
  wishlisted_at timestamp with time zone,
  created_at timestamp with time zone,
  updated_at timestamp with time zone,
  CONSTRAINT student_whishlist_pkey PRIMARY KEY (id),
  CONSTRAINT student_whishlist_student_id_fkey FOREIGN KEY (student_id) REFERENCES public.students(student_id)
);
CREATE TABLE public.students (
  student_id character varying NOT NULL,
  student_name character varying,
  student_email character varying UNIQUE,
  student_phone character varying UNIQUE,
  parents_number character varying,
  whatsapp character varying,
  assigned_counsellor_id character varying,
  assigned_counsellor_l3_id character varying,
  mode character varying DEFAULT 'Regular'::character varying,
  preferred_stream ARRAY,
  preferred_budget character varying DEFAULT 0,
  preferred_degree ARRAY,
  preferred_level ARRAY,
  preferred_specialization ARRAY,
  preferred_city ARRAY,
  preferred_state ARRAY,
  source character varying,
  first_source_url text,
  student_secondary_email character varying,
  student_current_city character varying,
  student_current_state character varying,
  is_connected_yet boolean DEFAULT false,
  number_of_unread_messages integer DEFAULT 0,
  assigned_l3_date timestamp with time zone,
  calling_status_l3 character varying,
  sub_calling_status_l3 character varying,
  remarks_count integer DEFAULT 0,
  remarks_l3 text,
  next_call_date_l3 timestamp with time zone,
  last_call_date_l3 timestamp with time zone,
  next_call_time_l3 character varying,
  is_connected_yet_l3 boolean DEFAULT false,
  total_remarks_l3 integer DEFAULT 0,
  first_callback_l2 timestamp with time zone,
  first_callback_l3 timestamp with time zone,
  first_form_filled_date timestamp with time zone,
  online_ffh integer DEFAULT 0,
  is_reactivity boolean DEFAULT false,
  created_at timestamp with time zone DEFAULT (CURRENT_TIMESTAMP AT TIME ZONE 'Asia/Kolkata'::text),
  updated_at timestamp with time zone,
  assigned_team_owner_date timestamp with time zone,
  assigned_team_owner_id character varying,
  preferred_university character varying,
  highest_degree character varying,
  completion_year character varying,
  current_profession character varying,
  current_role character varying,
  work_experience character varying,
  objective character varying,
  student_age integer DEFAULT 0,
  reassigneddate date,
  is_opened boolean DEFAULT false,
  is_transfered boolean DEFAULT false,
  is_edited boolean DEFAULT false,
  edited_by character varying DEFAULT NULL::character varying,
  enrollment_counsellor_id character varying,
  enrollment_counsellor_id_date timestamp without time zone,
  current_student_status character varying,
  current_student_ni_sub_status character varying,
  CONSTRAINT students_pkey PRIMARY KEY (student_id),
  CONSTRAINT students_assigned_counsellor_l3_id_fkey FOREIGN KEY (assigned_counsellor_l3_id) REFERENCES public.counsellors(counsellor_id)
);
CREATE TABLE public.supervisors (
  supervisor_id character varying NOT NULL,
  supervisor_name character varying NOT NULL,
  supervisor_email character varying NOT NULL UNIQUE,
  supervisor_password character varying NOT NULL,
  supervisor_phone_number character varying,
  status character varying DEFAULT 'active'::character varying,
  supervisor_last_login timestamp with time zone,
  created_at timestamp with time zone,
  updated_at timestamp with time zone,
  is_logout boolean DEFAULT false,
  supervisor_real_password character varying,
  CONSTRAINT supervisors_pkey PRIMARY KEY (supervisor_id)
);
CREATE TABLE public.templates (
  id integer NOT NULL DEFAULT nextval('templates_id_seq'::regclass),
  template_name character varying NOT NULL UNIQUE,
  content_type USER-DEFINED NOT NULL,
  image text,
  carousel_images ARRAY,
  pdf_file text,
  pdf_url text,
  pdf_name text,
  location_link text,
  is_dynamic boolean DEFAULT false,
  placeholders jsonb DEFAULT '{}'::jsonb,
  created_at timestamp with time zone DEFAULT '2025-08-05 15:06:59.166+00'::timestamp with time zone,
  updated_at timestamp with time zone,
  CONSTRAINT templates_pkey PRIMARY KEY (id)
);
CREATE TABLE public.tickets (
  ticket_id uuid NOT NULL,
  ticket_remark character varying NOT NULL,
  issue_description text NOT NULL,
  issue_type USER-DEFINED NOT NULL,
  status character varying DEFAULT 'open'::enum_tickets_status,
  created_by character varying NOT NULL,
  closed_by character varying,
  closed_at timestamp with time zone,
  created_at timestamp with time zone,
  updated_at timestamp with time zone,
  is_overdue_alert_sent boolean DEFAULT false,
  overdue_alert_sent_at timestamp without time zone,
  CONSTRAINT tickets_pkey PRIMARY KEY (ticket_id)
);
CREATE TABLE public.universities_api_header (
  id integer NOT NULL DEFAULT nextval('universities_api_header_id_seq'::regclass),
  university_name character varying NOT NULL UNIQUE,
  headers jsonb NOT NULL DEFAULT '{}'::jsonb,
  created_at timestamp with time zone NOT NULL,
  updated_at timestamp with time zone NOT NULL,
  CONSTRAINT universities_api_header_pkey PRIMARY KEY (id)
);
CREATE TABLE public.universities_api_header_values (
  id integer NOT NULL DEFAULT nextval('universities_api_header_values_id_seq'::regclass),
  course_id character varying NOT NULL,
  university_name character varying NOT NULL,
  values jsonb NOT NULL DEFAULT '{}'::jsonb,
  created_at timestamp with time zone,
  CONSTRAINT universities_api_header_values_pkey PRIMARY KEY (id),
  CONSTRAINT universities_api_header_values_course_id_fkey FOREIGN KEY (course_id) REFERENCES public.university_courses(course_id)
);
CREATE TABLE public.university_brochures (
  id integer NOT NULL DEFAULT nextval('university_brochures_id_seq'::regclass),
  university_name character varying,
  brochure_url character varying,
  created_at timestamp with time zone,
  updated_at timestamp with time zone,
  CONSTRAINT university_brochures_pkey PRIMARY KEY (id)
);
CREATE TABLE public.university_courses (
  course_id character varying NOT NULL,
  university_name character varying,
  university_state character varying,
  university_city character varying,
  degree_name character varying,
  specialization character varying,
  stream character varying,
  level character varying,
  course_name character varying,
  total_fees numeric,
  semester_fees numeric,
  annual_fees numeric,
  study_mode character varying,
  duration character varying,
  duration_type character varying,
  created_at timestamp with time zone,
  updated_at timestamp with time zone,
  status character varying DEFAULT 'active'::character varying,
  brochure_url character varying,
  usp ARRAY DEFAULT '{}'::text[],
  eligibility ARRAY DEFAULT '{}'::text[],
  CONSTRAINT university_courses_pkey PRIMARY KEY (course_id)
);
CREATE TABLE public.user_activity_logs (
  id bigint NOT NULL DEFAULT nextval('user_activity_logs_id_seq'::regclass),
  user_id character varying,
  endpoint character varying NOT NULL,
  method character varying NOT NULL,
  request_data json,
  response_data json,
  status_code integer,
  ip_address character varying,
  user_agent character varying,
  referrer character varying,
  location json,
  createdAt timestamp with time zone NOT NULL,
  updatedAt timestamp with time zone NOT NULL,
  CONSTRAINT user_activity_logs_pkey PRIMARY KEY (id)
);
CREATE TABLE public.website_chat_messages (
  id character varying NOT NULL,
  chat_id character varying,
  sender_type USER-DEFINED NOT NULL,
  sender_user_id character varying,
  display_name character varying,
  content text NOT NULL,
  is_read boolean DEFAULT false,
  read_at timestamp with time zone,
  metadata jsonb DEFAULT '{}'::jsonb,
  created_at timestamp with time zone NOT NULL,
  CONSTRAINT website_chat_messages_pkey PRIMARY KEY (id),
  CONSTRAINT website_chat_messages_chat_id_fkey FOREIGN KEY (chat_id) REFERENCES public.website_chats(id)
);
CREATE TABLE public.website_chats (
  id character varying NOT NULL,
  student_id character varying,
  student_name character varying,
  student_phone character varying,
  counsellor_id character varying,
  display_name character varying,
  status USER-DEFINED NOT NULL DEFAULT 'PENDING_ACCEPTANCE'::enum_website_chats_status,
  last_message_at timestamp with time zone,
  unread_count_student integer DEFAULT 0,
  unread_count_counsellor integer DEFAULT 0,
  student_platform_details jsonb DEFAULT '{}'::jsonb,
  closed_by character varying,
  closed_reason character varying,
  created_at timestamp with time zone NOT NULL,
  updated_at timestamp with time zone NOT NULL,
  last_message character varying,
  CONSTRAINT website_chats_pkey PRIMARY KEY (id)
);